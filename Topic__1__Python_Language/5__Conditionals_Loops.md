# Conditionals (`if, elif, else`)

# Loops (`for, in, while`)

# In-line-if (Conditional assignment)

#### Example :
```python
age = 22
is_eligible_to_vote = "YES" if age > 18 else "NO"
```

# In-line-loop (List & Dictionary)
